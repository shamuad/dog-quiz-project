const reducer = (state = [], action = {}) => {
    switch (action.type) {
        // case SELECT:
        // console.log(SELECT)
        //   return [...state, action.payload]
        default:
            // console.log(state)
            return state
    }
}

export default reducer

